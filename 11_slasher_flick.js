function slasher(arr, howMany) {
  return arr.slice(howMany, arr.length);
}


//Same with comments
//funktion to to give back a given arrays elements staring with the given index
function slasher(arr, howMany) {
  //return the sliced portion of the original array
  //starting / chop the given number of elements till the end of the array
  return arr.slice(howMany, arr.length);
}
